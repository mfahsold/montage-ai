If this PR contains code health or housekeeping changes (dead code removal, linting, docs), please include a brief rationale and confirm that all relevant tests pass locally (run `make ci-local`).

Optional: link to issue tracking the triage (e.g., "issue #12").