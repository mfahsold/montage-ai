These workflows were intentionally disabled and removed from active GitHub Actions usage.

Rationale:
- This project enforces a strict **no GitHub Actions** policy.
- CI is run locally via `./scripts/ci.sh` or via a vendor-neutral CI (Jenkins/Drone/Buildkite).

The original workflow files were archived here for historical reference. Do not restore them into `.github/workflows/`.
