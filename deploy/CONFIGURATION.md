# Deployment Configuration

The canonical configuration reference lives in `docs/configuration.md`.

See:
- `docs/configuration.md#deployment-configuration`
- `deploy/config.env` (defaults used by deployment tooling)
