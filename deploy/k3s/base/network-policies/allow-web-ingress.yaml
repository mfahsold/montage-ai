apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-web-ingress
  namespace: montage-ai
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: montage-ai-web
  policyTypes:
  - Ingress
  ingress:
  - ports:
    - protocol: TCP
      port: 5000
