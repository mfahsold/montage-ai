---
apiVersion: v1
kind: Service
metadata:
  name: montage-ai-web
  namespace: montage-ai
  labels:
    app: montage-ai
    component: web
spec:
  selector:
    app: montage-ai
    component: web
  ports:
  - name: http
    protocol: TCP
    port: 8080
    targetPort: 8080
  type: ClusterIP
