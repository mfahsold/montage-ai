apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../base
- keda-scaledobjects.yaml
# Namespace is injected dynamically by deploy.sh via `kustomize edit set namespace`.
# Default matches CLUSTER_NAMESPACE in config-global.yaml / deploy/config.env.
namespace: montage-ai-test
