apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  # GPU-enabled render Job
  - path: patch-job-gpu.yaml
  # Optional: GPU for web UI (usually not required, kept minimal)
  - path: patch-web-gpu.yaml
