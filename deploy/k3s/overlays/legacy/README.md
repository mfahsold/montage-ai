# Legacy Overlays (Archived)

These overlays are retained for internal reference only.
They may be out of date and are not part of the canonical deployment flow.

Canonical deployment uses:
- `deploy/k3s/overlays/cluster`

For local development, use `./montage-ai.sh web` or Docker Compose.
