# Video Processing
moviepy>=2.1.1,<2.3  # 2.x uses .subclipped(); 2.2.x requires pillow<12
opencv-python-headless>=4.10.0
mediapipe>=0.10.0  # For Smart Reframing (Face Detection)
soundfile>=0.12.0
Pillow>=10.0.0,<12.0  # moviepy 2.2.x constraint
proglog>=0.1.10
tqdm>=4.66.0
scenedetect[opencv]>=0.6.4
requests>=2.32.0
numpy>=1.24.0
scipy>=1.10.0  # For mathematical path optimization (Smart Reframing)

# Color Matching (Shot-to-Shot consistency)
color-matcher>=0.5.0

# HTTP / API
# (requests already listed above)

# LLM API (cgpu/Gemini integration)
openai>=1.55.0

# System Monitoring
psutil>=6.0.0

# Timeline Export (Professional NLE Integration)
OpenTimelineIO>=0.17.0  # Academy Software Foundation standard
jsonschema>=4.23.0  # JSON schema validation for Creative Director

# Web UI
Flask>=3.0.0
Werkzeug>=3.0.0

# Testing
pytest>=8.0.0
pytest-flask>=1.3.0
redis>=5.0.0
